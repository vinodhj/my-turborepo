{
  "name": "cf-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev  --name cf-worker --port 8686 --inspector-port 9697",
    "format": "prettier --write \"**/*.{ts,tsx,md,json,mts}\" --no-error-on-unmatched-pattern",
    "start": "wrangler dev",
    "test": "vitest run",
    "cf-typegen": "wrangler types",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "@cloudflare/workers-types": "^4.20240614.0",
    "typescript": "^5.4.5",
    "vitest": "1.5.0",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "graphql-tag": "^2.12.6",
    "graphql-yoga": "^5.4.0"
  }
}
